# Everlive JavaScript SDK

To use Everlive include **everlive.js** or **everlive.min.js** in your application. **everlive.js** comes with a source
map for debugging purposes.

HTML
```html
<script src="path/to/everlive/everlive.js"></script>
```

Nodejs
```javascript
var Everlive = require('path/to/everlive/everlive.js');
//or
var Everlive = require('everlive-sdk')
//if installed as npm module
```

Requirejs
```javascript
define(['Everlive'], function (Everlive) {
});
```
